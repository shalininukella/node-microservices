default: Date.now runs only once, on creation.

pre('save') keeps updatedAt accurate on every save.

💡 If you want a more automated solution, Mongoose 7+ supports:



{ timestamps: true }


const userSchema = new mongoose.Schema({
  ...
}, { timestamps: true });

That will automatically add and manage both createdAt and updatedAt without needing custom middleware.